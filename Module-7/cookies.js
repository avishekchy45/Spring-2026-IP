// Set a cookie
document.cookie = "username=Steve Jobs";

// Read cookies
console.log(document.cookie);

// Set an expiration date for the cookie
document.cookie = "username=Mark Zuckerberg; max-age=3600; path=/";
